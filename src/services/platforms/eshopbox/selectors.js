export const SELECTORS = {
  emailInput: 'input[type="email"]',
  continueBtn: 'text=Continue',
  otpInput: 'input[name="vcode"], input[autocomplete="one-time-code"], input[name*="otp" i], input[placeholder*="code" i], input[type="tel"]',
  verifyBtn: 'button[type="submit"]',

  // AFTER LOGIN
  invoicePageNav: 'text=Billing', // or "Invoices" (adjust)
  downloadBtn: 'text=Download',   // adjust based on UI
};
